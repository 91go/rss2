name: test and deploy
on:
  push:
    tags:
      - v*
    branches:
      - master
      - main
  pull_request:
permissions:
  contents: read

jobs:
  test:
    name: Test
    strategy:
      matrix:
        go-version: [1.16.x, 1.17.x]
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Set up Go
        uses: actions/setup-go@v1
        with:
          go-version: ${{ matrix.go-version }}
      - name: checkout code
        uses: actions/checkout@v1
      - name: unit test
        run: go test ./...

#      - name: Upload Coverage report to CodeCov
#        uses: codecov/codecov-action@v1.0.0
#        with:
#          token: ${{secrets.CODECOV_TOKEN}}
#          file: ./coverage.txt

      # 没有发布docker镜像，直接在服务器上构建&部署
      - name: deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          password: ${{ secrets.SERVER_SSH_PWD }}
          port: 22
          timeout: 10m
          script_stop: true
          script: |
            whoami
  #            cd ~/data/rss2 && git pull && docker-compose down && docker-compose up --build -d
  #            cd ~/data/rss2 && git pull && docker-compose up -d --force-recreate rss2-service
            cd ~/data/rss2 && git pull && docker-compose rm -sf rss2 && docker rmi rss2_rss2 && docker-compose up -d rss2


#  build:
#    name: Build
#    runs-on: ubuntu-latest
#    needs: [ lint, test ]
#    steps:
#      - name: Set up Go
#        uses: actions/setup-go@v1
#        with:
#          go-version: 1.17.x
#
#      - name: Check out code
#        uses: actions/checkout@v1
#
#      - name: Build
#        run: go build